As a Senior QA Engineer, I have reviewed the comprehensive Product Requirements Document (PRD) provided. My test cases strictly adhere to the explicit functional, non-functional, and security requirements mentioned in this document.

Here are 25 detailed test cases for the VWO Login Dashboard:

## VWO Login Dashboard Test Cases

| Test ID | Description | Pre-conditions | Steps | Expected Result | Priority |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_AUTH_001** | Successful login using Email and Password. | User account exists with active credentials. | 1. Navigate to the VWO Login Dashboard. 2. Enter valid Email address. 3. Enter valid Password. 4. Click 'Log In'. | User is redirected seamlessly to the main dashboard/VWO Core Platform (Seamless transition). A successful confirmation must occur. | High |
| **TC_AUTH_002** | Authentication using Enterprise SSO integration (SAML/OAuth). | Account provisioned via an integrated Identity Provider (IdP). | 1. Select 'Sign In with SSO' option. 2. Click the appropriate IdP provider button (e.g., Okta). 3. Complete the external authentication flow. | User is redirected to the VWO Dashboard upon successful validation by the IdP, confirming enterprise security adherence. | Critical |
| **TC_AUTH_003** | Login with invalid credentials (General Failure). | Credentials do not match any active account. | 1. Enter an incorrect Email address. 2. Enter a correct Password. 3. Click 'Log In'. | The system displays clear, actionable error messages indicating failure (e.g., "Invalid username or password."). No sensitive data is leaked about which field was wrong. | High |
| **TC_AUTH_004** | Login with empty mandatory fields (Email/Password). | User on the login form. | 1. Leave both Email and Password fields blank. 2. Click 'Log In'. | Real-time validation must trigger, displaying error messages for both missing fields before submission. Authentication must fail. | High |
| **TC_AUTH_005** | Testing real-time email format validation (Client-side). | User on the login form. | 1. Enter an input string that is not a valid email format (e.g., `user@domain`). 2. Click away from the field ('on blur'). | The system must trigger immediate visual feedback error message regarding the invalid email format (specialized mobile keyboards must be considered). | High |
| **TC_AUTH_006** | Testing password strength validation indicator. | User on the login form (if applicable for initial setup/change). | 1. Input a password that meets minimum complexity requirements, then change it to one that fails them incrementally. | The Password Strength Indicator must provide *visual feedback* reflecting the current status against enforced security standards (e.g., weak $\rightarrow$ medium $\rightarrow$ strong). | High |
| **TC_AUTH_007** | Functionality of 'Remember Me' checkbox for returning users. | User successfully logs in and checks 'Remember Me'. | 1. Check the 'Remember Me' box. 2. Close browser completely. 3. Reopen the browser later without credentials. | The user must be automatically logged back into the Dashboard (Persistent login session). | High |
| **TC_AUTH_008** | Functionality of 'Remember Me' checkbox when *unchecked*. | User successfully logs in, leaving the box unchecked. | 1. Uncheck the 'Remember Me' box. 2. Close browser completely. 3. Reopen the browser later without credentials. | The user must be forced to enter credentials again upon reopening the browser (No persistent login session). | High |
| **TC_AUTH_009** | Password Recovery via Email Reset Flow Initiation. | User is logged out. | 1. Click 'Forgot Password?'. 2. Enter a registered email address and submit. | The system must confirm initiation of the reset process (e.g., "Reset link sent to your email"). **Verification requires checking inbox.** | High |
| **TC_AUTH_010** | Testing password recovery using an invalid/non-registered email. | User on Forgot Password page. | 1. Enter a non-existent email address in the flow. 2. Submit. | The system must display an actionable, generic error message ("If an account exists with that email...") without confirming if the email is valid or not. | High |
| **TC_AUTH_011** | Session Management Timeout (Inactivity). | User logs in and begins interacting with the dashboard. | 1. Leave the active browser session idle for the time defined by the configured timeout period. 2. Attempt to click any element on the dashboard. | The system must automatically log out the user, requiring re-authentication, fulfilling secure session handling requirements. | Critical |
| **TC_AUTH_012** | Mandatory HTTPS Enforcement Check. | User attempts to access the login page via `http://` URL structure. | 1. Accessing `http://app.vwo.com/login`. | The browser must automatically redirect the user to the secure `https://` version of the URL, enforcing SSL/TLS encryption for all communications. | Critical |
| **TC_AUTH_013** | Rate Limiting enforcement against brute-force attempts. | User on login screen with no account access. | 1. Attempt to log in with an incorrect password (N times, where N is defined by rate limit). 2. On the N+1 attempt, try to log in again. | After reaching the threshold (Rate Limiting), the system must temporarily lock out further attempts, displaying a specific "Too many attempts" message. | Critical |
| **TC_UX_014** | Accessibility: Full Keyboard Navigation Test. | User on login screen. | 1. Navigate to the page using only the `Tab` key. 2. Attempt form submission without mouse interaction. | Every interactive element (labels, fields, buttons) must be reachable and operable sequentially using only the Tab, Shift+Tab, Enter, and Space keys. Focus indicators must be visible. | Critical |
| **TC_UX_015** | Accessibility: Screen Reader Compatibility Check. | User on login screen simulated via VoiceOver/JAWS. | 1. Navigate through all form fields and labels using the screen reader software. | All form inputs, buttons, and instructions must be correctly announced by the screen reader using appropriate ARIA labels, ensuring compliance with WCAG standards. | High |
| **TC_UX_016** | Responsiveness Check: Mobile Viewport (Touch Targets). | User on login screen viewed on a mobile emulator/device. | 1. Attempt to tap all interactive elements (buttons, links). | All touch targets must be sufficiently large and spaced apart (touch-friendly controls) to prevent misclicks on mobile devices. | High |
| **TC_UX_017** | Theme Support: Light Mode vs. Dark Mode Toggle. | User on the login page. | 1. Observe initial load in default mode (assumed Light). 2. Activate the 'Dark Mode' toggle. 3. Re-activate the toggle to return to Light Mode. | All text, background colors, and interactive elements must correctly adapt their contrast and color scheme for both modes without losing functionality or violating brand consistency. | Medium |
| **TC_UX_018** | Loading State Feedback Mechanism Check. | User clicks 'Log In' when the server processes credentials. | 1. Click Log In. 2. Observe the interface while waiting for the backend response (Simulated delay). | The system must display clear, persistent visual feedback (Loading spinner/overlay) to confirm that the request was received and is being processed, preventing double-submission. | High |
| **TC_SEC_019** | Data Protection: Encryption verification during transmission. | User inputs credentials. | 1. Monitor network traffic (using Developer Tools/Proxy). 2. Submit credentials. | The data payload transmitted from the client to the server must be fully encrypted, confirming SSL/TLS usage and protecting sensitive fields in transit. | Critical |
| **TC_SEC_020** | Data Protection: Password storage integrity check (Conceptual). | N/A (Requires backend access, but testable via output assumption). | 1. Observe the confirmation of password change or setup success message. | The system must confirm adherence to "encrypted password storage using industry-standard hashing algorithms" in its operational logs/behavioral checks. **(If a direct check is impossible, verify all error messages are generic enough to prevent data leakage)**. | High |
| **TC_SEC_021** | GDPR Compliance Check: Data subject request handling (Conceptual). | User initiates account deletion or export request (if link is present on dashboard/profile). | 1. Follow the documented process for a user requesting their data be deleted or exported. | The system must handle the request in compliance with GDPR, ensuring secure deletion or export of personal data upon validation. | High |
| **TC_PERF_022** | Page Load Speed Test (Baseline Performance). | User on Login Dashboard on standard connection bandwidth. | 1. Measure the time elapsed from navigating to `app.vwo.com` until all interactive elements are fully rendered. | The entire page must load and become usable within the specified target of **2 seconds**. | High |
| **TC_PERF_023** | Scalability Test: Concurrent User Simulation. | Load testing tools configured. | 1. Simulate a burst of thousands of simultaneous login attempts from various geographical points (Multi-region). | The system must maintain stability, processing the high volume of requests without service degradation or failure, supporting scalability goals. | High |
| **TC_INT_024** | Analytics Tracking Verification (Success Path). | User successfully logs in. | 1. Log in successfully using any method. 2. Check platform analytics tools/logs immediately after login. | The system must trigger the required 'Login Success' event, correctly recording relevant data points for platform optimization tracking. | High |
| **TC_INT_025** | Analytics Tracking Verification (Failure Path). | User fails login attempt multiple times. | 1. Force multiple failed login attempts within a short window. 2. Check platform analytics tools/logs immediately after the failure threshold is hit. | The system must accurately track the 'Login Failure' event, noting the source of failure, to support security monitoring and analysis. | High |